# SEO Agent Tools v0.1.2

This patch materializes the ignored repository-local `tmp/` workspace and reorganizes maintenance instructions for progressive reading and authorized coordination across the public repository and connected private server repository.

The runtime skill, public recipe catalog, recipe schemas, and result contracts are unchanged. This preparation does not create a release tag or publish a GitHub release.
