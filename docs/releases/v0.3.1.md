# SEO Agent Tools v0.3.1

## [v0.3.1]

This patch release republishes the `v0.3.0` portable runtime and catalog content after repository release immutability was enabled. The published release locks its associated tag and assets and includes GitHub's release attestation.

The public contract versions remain:

- portable skill and plugin packages: `0.3.1`;
- catalog: `0.3.0`;
- recipe schema: `2`;
- content-production recipe: `content-production-workflow@1.0.0`;
- five existing recipes: `2.0.0` each;
- result contracts: `seo-opportunity-set/v1`, `seo-diagnostic/v1`, and `seo-implementation-handoff/v1`.

No recipe, schema, result-contract, capability, evidence-boundary, or runtime-guidance content changed from `v0.3.0`. The sixth recipe continues to coordinate one selected content objective without writing finished copy, granting CMS authority, or treating a staged snapshot as production proof.

The release publishes three runtime archives plus `seo-agent-tools-catalog-v0.3.1.json` and `seo-agent-tools-catalog-manifest-v0.3.1.json`. The manifest records the exact immutable source commit and source checksums, and `SHA256SUMS` covers all five assets.

## Validation

The exact release candidate passed `npm run validate`, deterministic package replay, and the release preflight for `v0.3.1`. The content-production runtime is byte-identical to `v0.3.0`, whose fresh-context evaluation passed all 13 named invariants across activation, capability boundaries, and live publication verification.
